syntax = "proto3";

package proto;

import "google/protobuf/timestamp.proto";

enum Side {
    SELL = 0;
    BUY = 1;
}

enum Type {
    LIMIT = 0;
    MARKET = 1;
    STOP = 2;
}

message OrderRequest {
    string user_id = 1;
    string order_id = 2;
    uint64 amount = 3;
    uint64 price = 4;
    Side Side = 5;
    Type type = 6;
}

message OrderConf {
    string user_id = 1;
    string order_id = 2;
    uint64 amount = 3;
    uint64 price = 4;
    Side Side = 5;
    Type type = 6;
    string message = 7;
    google.protobuf.Timestamp created_at = 8;
}
